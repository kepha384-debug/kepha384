
{
  "entities": {
    "Comment": {
      "title": "Comment",
      "description": "A comment on a media item.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique ID of the comment." },
        "mediaId": { "type": "string", "description": "ID of the media item this comment belongs to." },
        "userId": { "type": "string", "description": "ID of the user who posted the comment." },
        "userName": { "type": "string", "description": "Name of the user who posted the comment." },
        "text": { "type": "string", "description": "The content of the comment." },
        "timestamp": { "type": "string", "format": "date-time", "description": "When the comment was posted." },
        "isApproved": { "type": "boolean", "description": "Whether the comment has been approved by an admin." }
      },
      "required": ["mediaId", "userId", "userName", "text", "timestamp", "isApproved"]
    }
  },
  "firestore": {
    "/comments/{commentId}": {
      "schema": "Comment",
      "description": "Collection of comments for media items."
    }
  }
}
